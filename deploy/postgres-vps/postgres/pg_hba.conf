# PostgreSQL is only on an internal Docker network. PgBouncer performs the
# public mTLS boundary; PostgreSQL still requires SCRAM for every TCP client.

local   all             all                                     scram-sha-256
host    all             all             0.0.0.0/0               scram-sha-256
host    all             all             ::0/0                   scram-sha-256
