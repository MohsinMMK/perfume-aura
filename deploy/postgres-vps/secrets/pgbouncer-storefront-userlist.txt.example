# The public mTLS pooler accepts only the two scoped storefront principals. Do
# not place an ops or owner verifier in this file.
#
# "storefront_runtime" "SCRAM-SHA-256$REPLACE_WITH_A_REAL_VERIFIER"
# "storefront_payment_finalizer" "SCRAM-SHA-256$REPLACE_WITH_A_REAL_VERIFIER"
